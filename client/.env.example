# Frontend environment — copy to client/.env and fill in. NEVER commit the real .env.

# Backend base URL (no trailing slash)
VITE_API_URL=http://localhost:3000

# Google OAuth client ID
VITE_GOOGLE_CLIENT_ID=
